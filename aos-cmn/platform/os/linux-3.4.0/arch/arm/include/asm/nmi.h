#ifndef _ASM_ARM_NMI_H
#define _ASM_ARM_NMI_H

/* 
 * in include/linux/nmi.h, need asm/nmi.h if enable CONFIG_HAVE_NMI_WATCHDOG 
 * so just add the file because we have external touch_nmi_watchdog() in 
 * msm_watchdog.c 
 */

#endif /* _ASM_ARM_NMI_H */
