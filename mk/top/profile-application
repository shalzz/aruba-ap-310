ifdef PROFILE_APPLICATION
NEVER_BUILD_DSO=1
  ifndef WIN32
CFLAGS_PROF := -pg -g -DPROFILE_APPLICATION
LDFLAGS_PROF := -pg -g 
  else # WIN32
CFLAGS_PROF :=
LDFLAGS_PROF := -PROFILE
  endif # WIN32
endif
